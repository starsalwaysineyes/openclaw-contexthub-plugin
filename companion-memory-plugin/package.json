{
  "name": "openclaw-contexthub-memory-plugin",
  "version": "0.1.0",
  "description": "Companion OpenClaw memory-slot plugin for ContextHub",
  "author": "MemTensor",
  "license": "MIT",
  "type": "module",
  "main": "index.ts",
  "files": [
    "index.ts",
    "src",
    "README.md",
    "tsconfig.json"
  ],
  "openclaw": {
    "id": "openclaw-contexthub-memory-plugin",
    "extensions": [
      "./index.ts"
    ],
    "installDependencies": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "smoke": "tsc -p tsconfig.json && node dist/smoke.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
