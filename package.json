{
  "name": "openclaw-contexthub-plugin",
  "version": "0.2.0",
  "description": "OpenClaw ctx plugin for local paths and ContextHub phase-1 ctx:// filesystem operations",
  "author": "MemTensor",
  "license": "MIT",
  "type": "module",
  "main": "index.ts",
  "files": [
    "index.ts",
    "src",
    "README.md",
    "tsconfig.json"
  ],
  "openclaw": {
    "id": "openclaw-contexthub-plugin",
    "extensions": [
      "./index.ts"
    ],
    "installDependencies": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
